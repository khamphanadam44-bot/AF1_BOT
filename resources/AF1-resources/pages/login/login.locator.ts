/**
 * ======================================================
 * ไฟล์: login.locator.ts
 * ======================================================
 *
 * หน้าที่ของไฟล์นี้
 *
 * เก็บ Locator ของช่อง Username ช่อง Password และปุ่ม Sign in
 * แยก Locator ออกจากขั้นตอนการทำงาน เพื่อให้แก้ตำแหน่ง Element ได้จากไฟล์เดียวเมื่อหน้าเว็บเปลี่ยน
 *
 * หมายเหตุ:
 * ไฟล์นี้อธิบายหน้าที่ของ Code เท่านั้น การแก้ Comment ไม่มีผลต่อการทำงานของระบบ
 * ======================================================
 */

export const loginLocator = { 
    usernametxt : "#username",
    passwordtxt : "#password",
    submitbtn  : "#sign-in"
};